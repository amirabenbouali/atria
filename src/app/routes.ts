export const routes = {
  root: '/',
  calendar: '/calendar',
  today: '/today',
  insights: '/insights',
  tasks: '/tasks',
  goals: '/goals',
  projects: '/projects',
  settings: '/settings',
} as const;
